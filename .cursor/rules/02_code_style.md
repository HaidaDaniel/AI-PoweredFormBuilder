# Code style

- Prefer small functions, early returns.
- Use named exports for utilities.
- All server-only modules must end with `.server.ts`.
- No business logic inside React components.
- Always handle empty/error states in UI.
- Keep components in app/components with feature folders.
